各位同学大家好，我们现显示的三角形是通过这样的一组顶点数据来做的，也就是说你看位置信息和颜色是混搭的，那么我们所做出来的这个 binding description 也只有一个描述，对吧？那么我们现在来做一个实验，我们把数据拆成两份，一个是顶点，一个是颜色，这样的话应该怎么做呢？我们其实只需要来修改这一部分就可以了，我们来看一看，把这个data，我们把它注掉，加一个 STD Vector float，这个是非常单纯的数组，叫做 position 色，那么我们在一个 STD Vector。

load 那么叫做 m colors。好，然后我们来初始化这两个数组，到这里先用一下 m positions，等于我们把这 position 都拷进来吧？一个顶点，这是两个顶点，那么这个是 3 个顶点。 m colors 等于好，这是一个顶点的颜色，那么两个顶点的颜色， 3 个顶点的颜色。好，我们把数据拷出来，把这边注掉。注释在哪边？这边好注掉它，然后我们做 buffer 的时候就不只是这样了，把这个position，这个 test buffer 注掉，我们加一个 buffer 叫做 position buffer，然后我们再加一个 color buffer，这样我们就有两个 buffer 了，对吧？好，然后我们把这两个 buffer 做一下初始化，其实跟这个是很像的，很像考过来这个是 m position buffer，那么这边的话就不是 data 了，对吧？要从这个 position 的里面拿。这个就是变成了flow，然后数据给过来同样一个操作，我们给到color，那么数据就是这样的。好，那么这两个 buffer 我们就给它做出来了，是 color buffer，OK，然后我们要开始修改这个description，对吧？它的数不一样了，因为我们现在有两个buffer，所以说 resize 应该变成了2。我们把这段先留在这里，方便大家以后查看代码，然后把这边专门拷出来一份来做啊。

绑定为 0 的位置，绑定的是一个buffer，我们决定绑定为 0 的位置，绑定 position size of，那么float。乘以3，对吧？x，y，z，那么这是一个数组，那么对于第二个数组应该是描述第二个数组这边是一，对吧？搬定的位置就成了一，它俩组成的数组里面，它在 1 号位，它在 0 号位，然后 stride 仍然是这样子，对吧？以三个 float 为一组，然后 inputrate 是这样没有问题，然后把它返回去。

好，接下来我们看一看这边有没有什么变化啊？这边唯一发生的变化是什么呢？其实是在这边 low 这个 bending 的位置，我们看对于第一个描述是描述的position，对吧？那么这个没有问题吗？这个其实是有问题的，这边也需要改。把它注释掉，然后拿过来，这时候 offset 就是 0 了，对不对？根本就不需要写什么offset，因为它整个数组都是 position 的信息，那这边它对于颜色的绑定，对颜色的描述就不能这样了，那么它应该是从单定为一的这个数组里面去读，然后把它 ok 为一读出来颜色，然后这边就要变成仍然是0，对吧？因为颜色，颜色的数组里面，颜色的 buffer 里面去读，当然那里面都是颜色，就不需要说跨过一个position。OK，现在我们这两个都修改完毕了。好，那么我们再看一看。这边是 get text buffer。那么这个东西我们也要做一下修改了，对不对？我们不能再是 gettextbuffer 了，我们应该是这边返回的，应该是一个数组，我们来写一下返回什么的数组，返回一个 buffer 的数组，那么就是 CB vector a buffer 这边的话就是那么叫做buffers，在 buffers 的话里面第一个位置我们放的是他把 binding 为 0 了，是吧？ binding 为0，那么这个位置放的是谁？ position buffer，然后 get buffer。

好，那 binding 为一的位置放的是 color buffer， get buffer，这就是所谓的binding，对吧？ binding 为0， binding 为一，其实就是这个数组的 binding 这个位置。给这buffers。好，那么在外面我们接收的这个位置其实是在哪里看一下？在这里，在这里我们就不能这么写了，导出掉我们直接就拿到了一个位 k buffer 的数组来model。诶 get 看一下。嗯，这名字取得不好，我们改个名字，不是叫textbuffer，应该叫 get buffers。嗯，这边改成textbuffers，加个小 s 就行，这样比较容易懂，就是跟顶点相关的所有的buffers。好，这样我们也做好了这个绑定，这 index 就不需要动。

好，我们来实验一下，等一下在此之前还需要做一下清理，刚才就算成功了也不要轻易相信，是吧？因为这个毕竟 semic 点 h 的反应速度是没有这么快的，我们毕竟是写到 model 点 h 里面了。再重新编译一下，生成好这个就没有问题了，看这样一个三角形就出来了，是不是关闭这个析构也没有问题啊？所以说像通过这样一波操作我们就知道了，原来 bending 是就是这么回事，是吧？ 0 号位、 1 号位就这么简单，然后这边也是描述相关的attribute，如果拆开的话就是这样一个写法。好，那么这节课就到这里。

